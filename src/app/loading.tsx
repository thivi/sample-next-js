import { ReactElement } from "react";
import { CircularProgress } from "./(components)/oxygen";

const Loading = (): ReactElement => {
    return (
        <CircularProgress />
    );
};

export default Loading;
